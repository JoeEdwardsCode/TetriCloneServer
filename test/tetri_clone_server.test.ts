import { expect as expectCDK, matchTemplate, MatchStyle } from '@aws-cdk/assert';
import * as cdk from '@aws-cdk/core';
import * as TetriCloneServer from '../lib/tetri_clone_server-stack';

test('TBD Stack', () => {
    // QUESTIONS:
    //  1. What is most valuable to test here?
    //  2. How will this fit into future deployment pipelines?
});
